class GroceryItem {
  constructor (name, amount){
    this.name = name;
    this.amount = amount;
  }
  foodItem() {
    return 'you have chosen' $(this.name) ;
  }
  if(amount == null) {
    return 'there is 1' $(this.name) ;
  }
}
